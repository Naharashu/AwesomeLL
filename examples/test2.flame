use stdlib;

func add(i32 a, i32 b) i32 {
    return a+b;
}

func main() i32 {
    comptime f64 a = 3.14;
    comptime u64 b = 2;
    print(fmod(a, b));
    comptime string str = "Hi ^^";
    print(str);
    return 0;
}