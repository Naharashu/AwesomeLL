use stdlib;

func add(i32 a, i32 b) i32 {
    return a+b;
}

func main() i32 {
    comptime f64 a = 3;
    comptime f64 b = a + 0.14;
    comptime u64 c = 2;
    print(fmod(b, c));
    comptime string str = "Hi ^^";
    print(str);
    return 0;
}